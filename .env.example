# Database credentials
# Copy this file to .env and fill in your values.
# .env is NOT committed to Git.

DB_ROOT_PASSWORD=changeme_root
DB_NAME=openbookmanager
DB_USER=bookmanager
DB_PASSWORD=changeme_bookmanager
